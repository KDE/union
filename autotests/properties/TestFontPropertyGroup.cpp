// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from autotest.cpp.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#include <QtTest>

#include <properties/FontPropertyGroup.h>

#include "CreateTestInstances.h"

using namespace Union::Properties;
using namespace Qt::StringLiterals;

class TestFontPropertyGroup : public QObject
{
    Q_OBJECT
private Q_SLOTS:
    void testNull()
    {
        auto property = std::make_unique<FontPropertyGroup>();

        // A null instance should not have any values for its properties.
        QVERIFY(!property->family().has_value());
        QVERIFY(!property->size().has_value());
        QVERIFY(!property->weight().has_value());
        QVERIFY(!property->style().has_value());
        QVERIFY(!property->stretch().has_value());
        QVERIFY(!property->capitalization().has_value());
        QVERIFY(!property->decorations().has_value());
        QVERIFY(!property->letterSpacing().has_value());
        QVERIFY(!property->wordSpacing().has_value());
    }

    void testEmpty()
    {
        auto property = FontPropertyGroup::empty();

        // An empty instance should only have values that are considered "empty".
        QCOMPARE(property->family().value(), emptyValue<QString>());
        QCOMPARE(property->size().value(), emptyValue<qreal>());
        QCOMPARE(property->weight().value(), emptyValue<QFont::Weight>());
        QCOMPARE(property->style().value(), emptyValue<QFont::Style>());
        QCOMPARE(property->stretch().value(), emptyValue<int>());
        QCOMPARE(property->capitalization().value(), emptyValue<QFont::Capitalization>());
        QCOMPARE(property->decorations().value(), emptyValue<Union::Properties::TextDecorations>());
        QCOMPARE(property->letterSpacing().value(), emptyValue<qreal>());
        QCOMPARE(property->wordSpacing().value(), emptyValue<qreal>());
    }

    void testHasAnyValue()
    {
        auto property = std::make_unique<FontPropertyGroup>();

        // An empty instance should not have any values for its properties.
        QVERIFY(!property->hasAnyValue());

        {
            QString value;
            property->setFamily(value);
            QVERIFY(property->hasAnyValue());
            property->setFamily(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            qreal value;
            property->setSize(value);
            QVERIFY(property->hasAnyValue());
            property->setSize(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            QFont::Weight value;
            property->setWeight(value);
            QVERIFY(property->hasAnyValue());
            property->setWeight(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            QFont::Style value;
            property->setStyle(value);
            QVERIFY(property->hasAnyValue());
            property->setStyle(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            int value;
            property->setStretch(value);
            QVERIFY(property->hasAnyValue());
            property->setStretch(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            QFont::Capitalization value;
            property->setCapitalization(value);
            QVERIFY(property->hasAnyValue());
            property->setCapitalization(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            Union::Properties::TextDecorations value;
            property->setDecorations(value);
            QVERIFY(property->hasAnyValue());
            property->setDecorations(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            qreal value;
            property->setLetterSpacing(value);
            QVERIFY(property->hasAnyValue());
            property->setLetterSpacing(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
        {
            qreal value;
            property->setWordSpacing(value);
            QVERIFY(property->hasAnyValue());
            property->setWordSpacing(std::nullopt);
            QVERIFY(!property->hasAnyValue());
        }
    }

    void testResolveProperties()
    {
        auto source = std::make_unique<FontPropertyGroup>();
        auto destination = std::make_unique<FontPropertyGroup>();

        QVERIFY(!source->hasAnyValue());
        QVERIFY(!destination->hasAnyValue());

        // Calling resolve on empty source and destination should have no effect.
        FontPropertyGroup::resolveProperties(source.get(), destination.get());

        QVERIFY(!destination->hasAnyValue());

        source->setFamily(QString{});
        source->setSize(qreal{});
        source->setWeight(QFont::Weight{});
        source->setStyle(QFont::Style{});
        source->setStretch(int{});
        source->setCapitalization(QFont::Capitalization{});
        source->setDecorations(Union::Properties::TextDecorations{});
        source->setLetterSpacing(qreal{});
        source->setWordSpacing(qreal{});

        QVERIFY(source->hasAnyValue());
        QVERIFY(!destination->hasAnyValue());

        FontPropertyGroup::resolveProperties(source.get(), destination.get());

        QVERIFY(destination->hasAnyValue());

        QCOMPARE(destination->family(), source->family());
        QCOMPARE(destination->size(), source->size());
        QCOMPARE(destination->weight(), source->weight());
        QCOMPARE(destination->style(), source->style());
        QCOMPARE(destination->stretch(), source->stretch());
        QCOMPARE(destination->capitalization(), source->capitalization());
        QCOMPARE(destination->decorations(), source->decorations());
        QCOMPARE(destination->letterSpacing(), source->letterSpacing());
        QCOMPARE(destination->wordSpacing(), source->wordSpacing());
    }

    void testDataStream()
    {
        auto group = testFontPropertyGroupInstance();

        QByteArray data;
        QDataStream writeStream(&data, QIODevice::WriteOnly);
        writeStream << group.get();

        QCOMPARE(writeStream.status(), QDataStream::Status::Ok);
        QVERIFY(data.size() > 0);

        QDataStream readStream(data);
        auto readGroup = std::make_unique<FontPropertyGroup>();
        readStream >> readGroup;

        QCOMPARE(readStream.status(), QDataStream::Status::Ok);
        QCOMPARE(*readGroup, *group);
    }
};

QTEST_MAIN(TestFontPropertyGroup)

#include "TestFontPropertyGroup.moc"