// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>


#include "LinePropertyGroup.h"

#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class OutlinePropertyGroupPrivate;

/*!
\class Union::Properties::OutlinePropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group containing properties of an element's outline.
*/
class UNION_EXPORT OutlinePropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    OutlinePropertyGroup();
    /*!
     * Copy constructor.
     */
    OutlinePropertyGroup(const OutlinePropertyGroup &other);
    /*!
     * Move constructor.
     */
    OutlinePropertyGroup(OutlinePropertyGroup &&other);
    ~OutlinePropertyGroup();

    /*!
     * Copy assignment operator.
     */
    OutlinePropertyGroup &operator=(const OutlinePropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    OutlinePropertyGroup &operator=(OutlinePropertyGroup &&other);

    /*!
     * \brief A property group containing the properties of a line.
     */
    LinePropertyGroup *left() const;

    /*!
     * Set the value of left.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setLeft(std::unique_ptr<LinePropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a line.
     */
    LinePropertyGroup *right() const;

    /*!
     * Set the value of right.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setRight(std::unique_ptr<LinePropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a line.
     */
    LinePropertyGroup *top() const;

    /*!
     * Set the value of top.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setTop(std::unique_ptr<LinePropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a line.
     */
    LinePropertyGroup *bottom() const;

    /*!
     * Set the value of bottom.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setBottom(std::unique_ptr<LinePropertyGroup> &&newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const OutlinePropertyGroup *source, OutlinePropertyGroup *destination);

    /*!
     * Create and return an empty OutlinePropertyGroup instance.
     *
     * This will create an empty OutlinePropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<OutlinePropertyGroup> empty();

private:
    std::unique_ptr<OutlinePropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::OutlinePropertyGroup
 * Equality comparison for OutlinePropertyGroup.
 */
UNION_EXPORT bool operator==(const OutlinePropertyGroup &left, const OutlinePropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::OutlinePropertyGroup
 * QDebug support for OutlinePropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::OutlinePropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::OutlinePropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::OutlinePropertyGroup> &type);