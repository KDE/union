// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>



#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class AlignmentPropertyGroupPrivate;

/*!
\class Union::Properties::AlignmentPropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group with properties related to alignment.

*/
class UNION_EXPORT AlignmentPropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    AlignmentPropertyGroup();
    /*!
     * Copy constructor.
     */
    AlignmentPropertyGroup(const AlignmentPropertyGroup &other);
    /*!
     * Move constructor.
     */
    AlignmentPropertyGroup(AlignmentPropertyGroup &&other);
    ~AlignmentPropertyGroup();

    /*!
     * Copy assignment operator.
     */
    AlignmentPropertyGroup &operator=(const AlignmentPropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    AlignmentPropertyGroup &operator=(AlignmentPropertyGroup &&other);

    /*!
        The alignment container. This determines relative to what the element is placed.
     */
    std::optional<Union::Properties::AlignmentContainer> container() const;

    /*!
     * Set the value of container.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setContainer(const std::optional<Union::Properties::AlignmentContainer> &newValue);

    /*!
        The horizontal alignment.
     */
    std::optional<Union::Properties::Alignment> horizontal() const;

    /*!
     * Set the value of horizontal.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setHorizontal(const std::optional<Union::Properties::Alignment> &newValue);

    /*!
        The vertical alignment.
     */
    std::optional<Union::Properties::Alignment> vertical() const;

    /*!
     * Set the value of vertical.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setVertical(const std::optional<Union::Properties::Alignment> &newValue);

    /*!
        The order determines which elements come before others in the same alignment group.
     */
    std::optional<int> order() const;

    /*!
     * Set the value of order.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setOrder(const std::optional<int> &newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const AlignmentPropertyGroup *source, AlignmentPropertyGroup *destination);

    /*!
     * Create and return an empty AlignmentPropertyGroup instance.
     *
     * This will create an empty AlignmentPropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<AlignmentPropertyGroup> empty();

private:
    std::unique_ptr<AlignmentPropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::AlignmentPropertyGroup
 * Equality comparison for AlignmentPropertyGroup.
 */
UNION_EXPORT bool operator==(const AlignmentPropertyGroup &left, const AlignmentPropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::AlignmentPropertyGroup
 * QDebug support for AlignmentPropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::AlignmentPropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::AlignmentPropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::AlignmentPropertyGroup> &type);