// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>

#include <filesystem>

#include "../Color.h"

#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class ImagePropertyGroupPrivate;

/*!
\class Union::Properties::ImagePropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group describing properties of an image.

*/
class UNION_EXPORT ImagePropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    ImagePropertyGroup();
    /*!
     * Copy constructor.
     */
    ImagePropertyGroup(const ImagePropertyGroup &other);
    /*!
     * Move constructor.
     */
    ImagePropertyGroup(ImagePropertyGroup &&other);
    ~ImagePropertyGroup();

    /*!
     * Copy assignment operator.
     */
    ImagePropertyGroup &operator=(const ImagePropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    ImagePropertyGroup &operator=(ImagePropertyGroup &&other);

    /*!
        The path to an image to use as source for this image.
     */
    std::optional<std::filesystem::path> source() const;

    /*!
     * Set the value of source.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setSource(const std::optional<std::filesystem::path> &newValue);

    /*!
        The width to use for the image. If unset, the source's image width is used.
     */
    std::optional<qreal> width() const;

    /*!
     * Set the value of width.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setWidth(const std::optional<qreal> &newValue);

    /*!
        The height to use for the image. If unset, the source's image height is used.
     */
    std::optional<qreal> height() const;

    /*!
     * Set the value of height.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setHeight(const std::optional<qreal> &newValue);

    /*!
        An offset on placement of the image horizontally.
     */
    std::optional<qreal> xOffset() const;

    /*!
     * Set the value of xOffset.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setXOffset(const std::optional<qreal> &newValue);

    /*!
        An offset on placement of the image vertically.
     */
    std::optional<qreal> yOffset() const;

    /*!
     * Set the value of yOffset.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setYOffset(const std::optional<qreal> &newValue);

    /*!
        A set of flags to use for this image.
     */
    std::optional<Union::Properties::ImageFlags> flags() const;

    /*!
     * Set the value of flags.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setFlags(const std::optional<Union::Properties::ImageFlags> &newValue);

    /*!
        The color to use for rendering the image when the image is used as a mask.
     */
    std::optional<Union::Color> maskColor() const;

    /*!
     * Set the value of maskColor.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setMaskColor(const std::optional<Union::Color> &newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const ImagePropertyGroup *source, ImagePropertyGroup *destination);

    /*!
     * Create and return an empty ImagePropertyGroup instance.
     *
     * This will create an empty ImagePropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<ImagePropertyGroup> empty();

private:
    std::unique_ptr<ImagePropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::ImagePropertyGroup
 * Equality comparison for ImagePropertyGroup.
 */
UNION_EXPORT bool operator==(const ImagePropertyGroup &left, const ImagePropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::ImagePropertyGroup
 * QDebug support for ImagePropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::ImagePropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::ImagePropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::ImagePropertyGroup> &type);