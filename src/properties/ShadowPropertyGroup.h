// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>


#include "../Color.h"
#include "OffsetPropertyGroup.h"

#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class ShadowPropertyGroupPrivate;

/*!
\class Union::Properties::ShadowPropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group containing properties relating to an element's shadow.
*/
class UNION_EXPORT ShadowPropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    ShadowPropertyGroup();
    /*!
     * Copy constructor.
     */
    ShadowPropertyGroup(const ShadowPropertyGroup &other);
    /*!
     * Move constructor.
     */
    ShadowPropertyGroup(ShadowPropertyGroup &&other);
    ~ShadowPropertyGroup();

    /*!
     * Copy assignment operator.
     */
    ShadowPropertyGroup &operator=(const ShadowPropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    ShadowPropertyGroup &operator=(ShadowPropertyGroup &&other);

    /*!
     * The offset of the shadow from its element. Setting this will move the entire shadow,
as if the light were cast from a different angle.

     */
    OffsetPropertyGroup *offset() const;

    /*!
     * Set the value of offset.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setOffset(std::unique_ptr<OffsetPropertyGroup> &&newValue);

    /*!
        The color of the shadow.
     */
    std::optional<Union::Color> color() const;

    /*!
     * Set the value of color.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setColor(const std::optional<Union::Color> &newValue);

    /*!
        The size of the shadow. This expands the area used for the shadow compared to the element.
     */
    std::optional<qreal> size() const;

    /*!
     * Set the value of size.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setSize(const std::optional<qreal> &newValue);

    /*!
        The amount of blur to use for the shadow. Increasing this will increase how smooth the shadow
goes from fully transparent to the full shadow color.

     */
    std::optional<qreal> blur() const;

    /*!
     * Set the value of blur.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setBlur(const std::optional<qreal> &newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const ShadowPropertyGroup *source, ShadowPropertyGroup *destination);

    /*!
     * Create and return an empty ShadowPropertyGroup instance.
     *
     * This will create an empty ShadowPropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<ShadowPropertyGroup> empty();

private:
    std::unique_ptr<ShadowPropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::ShadowPropertyGroup
 * Equality comparison for ShadowPropertyGroup.
 */
UNION_EXPORT bool operator==(const ShadowPropertyGroup &left, const ShadowPropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::ShadowPropertyGroup
 * QDebug support for ShadowPropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::ShadowPropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::ShadowPropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::ShadowPropertyGroup> &type);