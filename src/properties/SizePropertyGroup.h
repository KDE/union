// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>

#include <QMarginsF>


#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class SizePropertyGroupPrivate;

/*!
\class Union::Properties::SizePropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group representing a set of sizes for cardinal directions.

*/
class UNION_EXPORT SizePropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    SizePropertyGroup();
    /*!
     * Copy constructor.
     */
    SizePropertyGroup(const SizePropertyGroup &other);
    /*!
     * Move constructor.
     */
    SizePropertyGroup(SizePropertyGroup &&other);
    ~SizePropertyGroup();

    /*!
     * Copy assignment operator.
     */
    SizePropertyGroup &operator=(const SizePropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    SizePropertyGroup &operator=(SizePropertyGroup &&other);

    /*!
        The size of the left side.
     */
    std::optional<qreal> left() const;

    /*!
     * Set the value of left.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setLeft(const std::optional<qreal> &newValue);

    /*!
        The size of the right side.
     */
    std::optional<qreal> right() const;

    /*!
     * Set the value of right.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setRight(const std::optional<qreal> &newValue);

    /*!
        The size of the top side.
     */
    std::optional<qreal> top() const;

    /*!
     * Set the value of top.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setTop(const std::optional<qreal> &newValue);

    /*!
        The size of the bottom side.
     */
    std::optional<qreal> bottom() const;

    /*!
     * Set the value of bottom.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setBottom(const std::optional<qreal> &newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const SizePropertyGroup *source, SizePropertyGroup *destination);

    /*!
     * Create and return an empty SizePropertyGroup instance.
     *
     * This will create an empty SizePropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<SizePropertyGroup> empty();

    /*!
    Convert a Size property to QMarginsF.

    If any values are not set, they will use 0.0.
    */
    QMarginsF toMargins() const;

private:
    std::unique_ptr<SizePropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::SizePropertyGroup
 * Equality comparison for SizePropertyGroup.
 */
UNION_EXPORT bool operator==(const SizePropertyGroup &left, const SizePropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::SizePropertyGroup
 * QDebug support for SizePropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::SizePropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::SizePropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::SizePropertyGroup> &type);