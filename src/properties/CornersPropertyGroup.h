// SPDX-License-Identifier: LGPL-2.1-only OR LGPL-3.0-only OR LicenseRef-KDE-Accepted-LGPL
// SPDX-FileCopyrightText: 2024 Arjen Hiemstra <ahiemstra@heimr.nl>
//
// This file is automatically generated from property.h.j2.
// To regenerate, run `tools/propertygenerator/generate_properties.py`.

#pragma once

// clang-format off

#include <QDebug>


#include "CornerPropertyGroup.h"

#include "PropertiesTypes.h"

#include "union_export.h"

// clang-format on

namespace Union
{
namespace Properties
{

class CornersPropertyGroupPrivate;

/*!
\class Union::Properties::CornersPropertyGroup
\inmodule core
\ingroup core-properties

\brief A property group containing properties of a background's corners.
*/
class UNION_EXPORT CornersPropertyGroup
{
public:
    /*!
     * Default constructor. Constructs a null instance.
     *
     * A null instance in this case means an instance that does not have any
     * values for its properties. This includes property groups.
     */
    CornersPropertyGroup();
    /*!
     * Copy constructor.
     */
    CornersPropertyGroup(const CornersPropertyGroup &other);
    /*!
     * Move constructor.
     */
    CornersPropertyGroup(CornersPropertyGroup &&other);
    ~CornersPropertyGroup();

    /*!
     * Copy assignment operator.
     */
    CornersPropertyGroup &operator=(const CornersPropertyGroup &other);
    /*!
     * Move assignment operator.
     */
    CornersPropertyGroup &operator=(CornersPropertyGroup &&other);

    /*!
     * \brief A property group containing the properties of a corner.
     */
    CornerPropertyGroup *topLeft() const;

    /*!
     * Set the value of topLeft.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setTopLeft(std::unique_ptr<CornerPropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a corner.
     */
    CornerPropertyGroup *topRight() const;

    /*!
     * Set the value of topRight.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setTopRight(std::unique_ptr<CornerPropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a corner.
     */
    CornerPropertyGroup *bottomLeft() const;

    /*!
     * Set the value of bottomLeft.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setBottomLeft(std::unique_ptr<CornerPropertyGroup> &&newValue);

    /*!
     * \brief A property group containing the properties of a corner.
     */
    CornerPropertyGroup *bottomRight() const;

    /*!
     * Set the value of bottomRight.
     *
     * \a newValue The new value or \c{std::nullopt} to unset the value.
     */
    void setBottomRight(std::unique_ptr<CornerPropertyGroup> &&newValue);

    /*!
     * Returns if this property group has any value set.
     *
     * Note that for any property that is also a property group, this will also
     * check if that group has any value.
     */
    bool hasAnyValue() const;

    /*!
     * Returns if this property is considered empty.
     *
     * A property is considered empty if it has no values or if the values it
     * contains are all considered empty values.
     */
    bool isEmpty() const;

    /*!
     * Returns a string representation of this property group.
     *
     * This is intended for debugging.
     *
     * \a indentation The amount of indentation to apply. Defaults to 0.
     * \a flags A set of flags that control conversion behavior.
     */
    QString toString(int indentation = 0, ToStringFlags flags = ToStringFlags{}) const;

    /*!
     * Copy property values from source to destination if destination does not have a property value.
     *
     * This will recursively copy property values of grouped properties.
     *
     * \a source      The source property group to copy from.
     * \a destination The destination property group to copy to.
     */
    static void resolveProperties(const CornersPropertyGroup *source, CornersPropertyGroup *destination);

    /*!
     * Create and return an empty CornersPropertyGroup instance.
     *
     * This will create an empty CornersPropertyGroup instance, which is defined as
     * an instance with all of its values default-constructed. Note that this is
     * different from a default-constructed instance which will have all its
     * values unset.
     */
    static std::unique_ptr<CornersPropertyGroup> empty();

    struct CornerRadii {
        qreal topLeft = 0.0;
        qreal topRight = 0.0;
        qreal bottomLeft = 0.0;
        qreal bottomRight = 0.0;
    };

    /*! Returns the radii of all corners */
    CornerRadii radii() const;

private:
    std::unique_ptr<CornersPropertyGroupPrivate> d;
};

/*!
 * \relates Union::Properties::CornersPropertyGroup
 * Equality comparison for CornersPropertyGroup.
 */
UNION_EXPORT bool operator==(const CornersPropertyGroup &left, const CornersPropertyGroup &right);
}
}

/*!
 * \relates Union::Properties::CornersPropertyGroup
 * QDebug support for CornersPropertyGroup.
 */
UNION_EXPORT QDebug operator<<(QDebug debug, Union::Properties::CornersPropertyGroup *type);

UNION_EXPORT QDataStream &operator<<(QDataStream &stream, const Union::Properties::CornersPropertyGroup *type);
UNION_EXPORT QDataStream &operator>>(QDataStream &stream, std::unique_ptr<Union::Properties::CornersPropertyGroup> &type);